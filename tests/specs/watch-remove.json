[
  {
    "name": "Remove coin",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Your watchlist is empty — tap Add coin to create one."
            }
          }
        ],
        "send": {
          "callback": "watch:remove"
        }
      }
    ]
  }
]
