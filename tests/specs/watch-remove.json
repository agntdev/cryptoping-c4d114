[
  {
    "name": "Remove coin",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Select a coin from the user's watchlist to remove (inline options)"
            }
          }
        ],
        "send": {
          "callback": "watch:remove"
        }
      }
    ]
  }
]
